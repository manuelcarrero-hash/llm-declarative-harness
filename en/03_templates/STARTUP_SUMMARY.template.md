# Startup summary — compatibility

This file is temporarily preserved for references from earlier versions. The only normative startup view is [`LOAD_RECEIPT.template.md`](LOAD_RECEIPT.template.md), which combines the plain explanation and auditable record.

Do not generate both artifacts or ask the user to complete this template.
